---
import ImageLightbox from '../islands/ImageLightbox.tsx';

interface Props {
	image?: string;
	title: string;
}

const { image, title } = Astro.props;
---

{image && (
	<ImageLightbox
		client:idle
		image={image}
		title={title}
	/>
)}
