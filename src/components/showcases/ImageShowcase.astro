---
import ImageLightbox from '../islands/ImageLightbox.tsx';

interface Props {
	image?: string;
	title: string;
	caption?: string;
}

const { image, title, caption } = Astro.props;
---

{image && (
	<ImageLightbox
		client:idle
		image={image}
		title={title}
		caption={caption}
	/>
)}
